<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/app_background"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/textView1"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:background="@drawable/texto_background"
        android:shadowColor="@android:color/darker_gray"
        android:shadowDx="2"
        android:shadowDy="1.5"
        android:shadowRadius="1.5"
        android:text="Olá, Sumano! Para pesquisar por voz, clique no botão teba abaixo!"
        android:textColor="@android:color/black"
        android:textSize="18dp" />

    <ImageButton
        android:id="@+id/botaoPesquisaPorVoz"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textView1"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="106dp"
        android:background="@drawable/seletor_botao"
        android:onClick="pesquisarComVoz"
        android:src="@drawable/mic" />

</RelativeLayout>